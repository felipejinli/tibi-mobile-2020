import React from 'react';
import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

function RightButton() {
  return (
    <Icon name="bell" size={25} style={{paddingRight: 10, color: 'white'}} />
  );
}

export default RightButton;
